apiVersion: batch/v1
kind: Job
metadata:
  name: db-migrate #generateName
spec:
  template:
    spec:
      restartPolicy: Never
      containers:
        - name: db-migrate
          image: builder-dev3
          imagePullPolicy: IfNotPresent
          command: ["bundle"]
          args: ["exec", "rails", "db:migrate"]
          envFrom:
            - secretRef:
                name: secrets
            - configMapRef:
                name: config
